mixin sectionBonus(img, imgMob)
    div(class='section-bonus__item')
        picture
            source(class='section-bonus__img', media="(min-width: 640px)", srcset=`${img}`)
            img(class='section-bonus__img', src=`${imgMob}`, alt="image bonus")
-for (var i = 0; i < bonuses.length; i++)
    -var d = bonuses[i]
    +sectionBonus(d.img, d.imgMob)